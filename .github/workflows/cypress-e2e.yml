name: 🧪 Pruebas E2E (Cypress)

on:
  pull_request:
    branches:
      - main

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    # 1. Levantar una base de datos PostgreSQL temporal para las pruebas
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Descargar código
        uses: actions/checkout@v3

      - name: Configar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # 2. Configurar Backend
      - name: Instalar y migrar Backend
        run: |
          cd backend
          npm install
          npx prisma generate
          npx prisma migrate deploy
          npm run build
        env:
          DATABASE_URL: postgresql://postgres:password@localhost:5432/test_db?schema=public
          JWT_SECRET: test_secret_cypress
          NODE_ENV: test

      # 3. Configurar Frontend
      - name: Instalar Frontend
        run: |
          cd frontend
          npm install
        env:
          VITE_API_URL: http://localhost:3000/api

      # 4. Correr Cypress
      - name: Cypress E2E
        uses: cypress-io/github-action@v6
        with:
          working-directory: frontend
          start: npm --prefix ../backend start, npm run preview
          wait-on: 'http://localhost:3000/api/health, http://localhost:4173'
          wait-on-timeout: 60
